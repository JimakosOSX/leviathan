---
# Disabled because fetch module does not seem to work properly.
# - hosts: old
#   remote_user: jim
#   become: yes
#   vars_files:
#   - ../vars/main.yml
#   roles:
#   - { role: general, create_backup: true }

- hosts: new
  remote_user: ubuntu
  become: true
  vars_files:
  - ../vars/main.yml
  roles: 
    - { role: general, setup: True }
    - role: mariadb
    - role: postfix
    - role: dovecot
    - { role: general, restore_backup: True }
    - role: zerotier
    - { role: firewalld, vpn_tunnel_enabled: True }
